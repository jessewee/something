import 'package:flutter/material.dart';

/// 找回密码页
class RetrievePwdPage extends StatelessWidget {
  static const routeName = '/retrieve_pwd';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
